# [unstable]
# build-std = ["core", "alloc", "compiler_builtins"]
# build-std-features = ["compiler-builtins-mem"]
#
# [build]
# target = "./x86_64-unknown-bmvm.json"
# rustflags = "-Clink-arg=-Tlayout.x -Crelocation-model=static"